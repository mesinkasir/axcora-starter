<div class="hero">
  <div class="hero-content">
    <h1 class="text-light">{{title}}</h1>
    <p>{{text}}</p>
    <div class="hero-actions">
      <a href="{{button1_url}}" class="btn btn-{{button1_color}}">{{button1}}</a>
      <a href="{{button2_url}}" class="btn btn-{{button2_color}}">{{button2}}</a>
    </div>
  </div>
</div>
